name: Plugin Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    name: Plugin Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Abhängigkeiten installieren
        run: pip install pytest cryptography pytest-json-report

      - name: Tests ausführen
        run: |
          pytest tests/ -v --tb=short \
            --json-report --json-report-file=pytest-report.json
        continue-on-error: true

      - name: PR Kommentar posten
        if: github.event_name == 'pull_request'
        run: python3 .github/scripts/pr_review_analyzer.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Exit mit Testergebnis
        run: pytest tests/ --tb=no -q
